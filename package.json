{
  "name": "undefined",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i src/styles.css -o src/public/css/styles.css",
    "build": "npm run build:css && eleventy",
    "dev": "npm run build:css && eleventy --serve",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@tailwindcss/cli": "^4.1.18",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
